drop table if exists `USER`;
create table `USER`(
    id INT auto_increment,
    name varchar(255),
    email varchar(255),
    password varchar(255),
    age varchar(255),
    gender varchar(255)
);
